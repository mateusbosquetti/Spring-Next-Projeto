import { Layout } from "components/layout"

export const CadastroCliente: React.FC = () => {
    return (
        <div>
            <Layout titulo="Clientes">
                Form
            </Layout>
        </div>
    )
}